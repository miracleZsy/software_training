import axiosUtil from '../../../../lib/axiosUtil';
import { message } from 'antd';
import * as actions from '../actions';

const fetchSharedCustomerUrl = '';

// Todo
// export const fetchSharedCustomer = (time = 0, page = 1) => (dispatch) => axiosUtil('post', fetchSharedCustomerUrl, {
//     time,
//     page, 
// }).then((value) => {
//     if (value !== undefined) {
//         dispatch(actions.fetchSharedCustomer(value.xxx));
//     }
// });

// Todo
export const deleteSharedCustomer = () =>  {
    
};
